<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="{{ description }}">
    <meta name="author" content="{{ author_email }}">

    <title>{{ page_title }}</title>

    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="css/1-col-portfolio.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

</head>

<body>

    {% include 'nav.html' %}
    
    <!-- Page Content -->
    <div class="container">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">KIS Staff Hardware Recommendations</h1>
            </div>
        </div>
        <!-- /.row -->

        {% for product in products %}
        <!-- {{ product.name }} -->
        <div class="row">

            <div class="col-md-7">
                <a href="{{ product.href }}">
                    <img class="img-responsive" src="{{ product.image_href }}" alt="{{ product.name }}" style="width:100%;">
                </a>
            </div>
            <div class="col-md-5">
                <h3>{{ product.name }}</h3>
                <h4 class="pull-right">{{ product.price }}</h4>
                <h4>{{ product.short_description }}</h4>
                <p>
                    <ul>
                        {% for highlight in product.highlights %}
                        <li>{{ highlight }}</li>
                        {% endfor %}
                    </ul>
                </p>
                <a class="btn btn-primary" href="{{ product.href }}">View Details <span class="glyphicon glyphicon-chevron-right"></span></a>
            </div>
        </div>
        <!-- /.row -->

        <hr>
        {% endfor %}

        <div class="row" id="ADP">
            <h4>* The Accidental Damage Protection (ADP) Warranty</h4>
            <p>The ADP warranty is standard on all of our laptops, providing complete coverage in the case of accidental damage as well as manufacturer defects. Drops, liquid spills, defenestration - it covers every situation.</p>
        </div>
        
        <!-- Footer -->
        <footer>
            <div class="row">
                <div class="col-lg-12">
                    <p>Copyright &copy; Kellogg Information Systems 2017</p>
                </div>
            </div>
            <!-- /.row -->
        </footer>

    </div>
    <!-- /.container -->

    <!-- jQuery -->
    <script src="js/jquery.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="js/bootstrap.min.js"></script>

</body>

</html>
